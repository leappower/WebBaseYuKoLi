# 多语言（i18n）体系完整指南

## 支持语言列表

共 **25 种语言**，统一在 `src/lang-registry.js` 管理：

| 分组 | 语言代码 | 原生名称 | 产品翻译状态 |
|------|---------|---------|:-----------:|
| 常用 | `zh-CN` | 中文（简体） | ✅ 完整（数据源） |
| 常用 | `zh-TW` | 中文（繁體） | ✅ |
| 常用 | `en` | English | ✅ |
| RTL | `ar` | العربية | ✅ |
| RTL | `he` | עברית | ✅ |
| 欧洲 | `de` | Deutsch | ✅ |
| 欧洲 | `es` | Español | ✅ |
| 欧洲 | `fr` | Français | ✅ |
| 欧洲 | `it` | Italiano | ✅ |
| 欧洲 | `nl` | Nederlands | ✅ |
| 欧洲 | `pl` | Polski | ✅ |
| 欧洲 | `pt` | Português | ✅ |
| 欧洲 | `ru` | Русский | ✅ |
| 欧洲 | `tr` | Türkçe | ✅ |
| 亚洲 | `ja` | 日本語 | ✅ |
| 亚洲 | `ko` | 한국어 | ✅ |
| 亚洲 | `id` | Bahasa Indonesia | ✅ |
| 亚洲 | `ms` | Bahasa Melayu | ✅ |
| 亚洲 | `fil` | Filipino | ✅ |
| 亚洲 | `th` | ภาษาไทย | ✅ |
| 亚洲 | `vi` | Tiếng Việt | ✅ |
| 亚洲 | `hi` | हिन्दी | ⚠️ 产品翻译为空，fallback zh-CN |
| 亚洲 | `my` | မြန်မာဘာသာ | ⚠️ 产品翻译为空，fallback zh-CN |
| 亚洲 | `km` | ភាសាខ្មែរ | ⚠️ 产品翻译为空，fallback zh-CN |
| 亚洲 | `lo` | ພາສາລາວ | ⚠️ 产品翻译为空，fallback zh-CN |

> `hi/km/my/lo` 的 UI 翻译已完整，但 `-product.json` 为空 `{}`，产品页面自动降级显示中文。执行 `npm run product:sync` 可生成占位翻译后续补全。

---

## 翻译文件结构

每种语言对应两个文件，放在 `src/assets/lang/`：

```
src/assets/lang/
├── zh-CN-ui.json        # UI 文本（导航、按钮、标签等，约 16KB）
├── zh-CN-product.json   # 产品文本（名称、参数、描述等，约 200KB）
├── en-ui.json
├── en-product.json
├── ...（其余 23 种语言）
└── languages.json       # 语言列表元数据
```

**分离设计的原因：**
- UI 文件小（16KB），首屏加载必须
- 产品文件大（200KB），按需加载，不影响首屏 TTI
- 两个文件可独立更新，互不干扰

---

## 翻译加载机制（TranslationManager）

### 加载流程

```
用户访问页面 / 切换语言
        ↓
loadTranslations(lang)
  ├── 命中 translationsCache → 直接返回（无 fetch）
  ├── 命中 pendingLoads → 等待同一 Promise（防并发重复请求）
  └── fetchTranslations(lang)
        ├── fetch ./assets/lang/{lang}-ui.json
        │     失败 → fallback fetch zh-CN-ui.json
        ├── fetch ./assets/lang/{lang}-product.json
        │     返回空 {} → fallback fetch zh-CN-product.json
        │     失败 → fallback fetch zh-CN-product.json
        ├── mergeTranslations(ui, product) → { ...ui, ...product }
        └── 写入 translationsCache，后续命中缓存直接返回
```

### 翻译键使用（前端 JS）

```javascript
// 所有翻译通过全局 tr() 函数调用（挂载在 window）
tr('nav_products', 'Products')
// → 当前语言有该键则返回翻译值，否则返回第二个参数作为 fallback

// 产品字段专用（支持 i18n 嵌套对象）
getProductI18nField(product, 'name', product.name)
```

### 翻译键使用（HTML 模板）

```html
<!-- 静态文本 -->
<span data-i18n="nav_products">Products</span>

<!-- placeholder 属性 -->
<input data-i18n-placeholder="search_placeholder" placeholder="Search...">

<!-- aria 属性 -->
<button data-i18n-aria-label="btn_close" aria-label="Close">×</button>
```

语言切换时，`TranslationManager` 通过 `MutationObserver` + `cachedElements` 批量更新所有带 `data-i18n` 属性的 DOM 元素。

### data-i18n 覆盖状态（截至 2026-03-17）

**2026-03-17 语言切换器三端专用变体（本次更新）：**

1. **Tablet 专用语言切换器变体**
   - 7 个 `*-tablet.html` 页面的语言切换器从 PC 版升级为独立的 Tablet 变体
   - **Tablet 变体特征**：按钮外观与 PC 版相同（文字+图标+箭头）；下拉面板改为 `fixed` 水平居中定位（`left-1/2 -translate-x-1/2 top-20`），宽度 `w-72`，`z-[200]`（介于 PC `z-50` 和 Mobile `z-[999]`）；面板顶部新增当前语言标签栏
   - 注释统一为 `<!-- Language Switcher (Tablet) -->`
   - 按钮内语言标签使用 `class="current-lang-btn-label"`（非 id），面板标题栏保持 `id="current-lang-label"` 作为 TranslationManager 的唯一更新目标

2. **三端切换器对照表**

   | 变体 | 页面后缀 | 按钮样式 | 面板定位 | z-index |
   |------|---------|---------|---------|---------|
   | PC | `-pc.html`, `-laptop.html` | 文字+图标+箭头，矩形 | `absolute right-0 top-full` | `z-50` |
   | Tablet | `-tablet.html` | 文字+图标+箭头，矩形 | `fixed left-1/2 -translate-x-1/2 top-20` | `z-[200]` |
   | Mobile | `-mobile.html`, `-mobile-unified.html` | 纯图标圆形 `size-10` | `fixed left-4 right-4 top-20` | `z-[999]` |

3. **TranslationManager 防御性检查（`translations.js`）**
   - `setupEventListeners()` 新增 `querySelectorAll('#lang-toggle-btn')` 多实例检测
   - 若同一页面出现多个 `#lang-toggle-btn`，控制台输出 `console.warn` 警告（只有第一个会被绑定）
   - `applyTranslations()` 新增同步更新 `.current-lang-btn-label`（Tablet 变体按钮标签）

---

**2026-03-17 多语言适配优化：**

1. **语言切换器 UI 全站上线**
   - 所有 30 个面向用户的 HTML 页面（排除 emails/linkedin）均已注入语言切换器组件
   - PC/Laptop/Tablet 版：导航栏右侧下拉选择器（`#lang-toggle-btn` + `#language-dropdown`），支持 25 种语言 + 搜索过滤
   - Mobile 版：导航栏图标按钮 + 浮层下拉，适配小屏

2. **JS 加载顺序修复**
   - 将 `lang-registry.js` 和 `translations.js` 移至所有其他 defer 脚本之前
   - 确保 `smart-popup.js` 等依赖 `translationManager` 的模块在运行时已能访问全局翻译实例

3. **TranslationManager 事件绑定修复（`translations.js`）**
   - `setupEventListeners()` 新增 `.lang-option` 点击 → `setLanguage(code)` 调用
   - 新增搜索输入框的 `input` 事件 → `filterLanguages(query)` 绑定
   - 新增 `#lang-toggle-btn` 点击 → `toggleLanguageDropdown()` 绑定
   - `openLanguageDropdown`/`closeLanguageDropdown` 统一改用 `display` 属性控制（与 HTML `style="display:none"` 保持一致）

4. **翻译 key 扩充**
   - `zh-CN-ui.json` / `en-ui.json` 新增 `lang_switcher_aria`（现共 546 个 key）

5. **home/index-pc.html data-i18n 补全**
   - 补充了 `home_hero_badge`、`home_hero_btn_roi`、`home_hw_view_inventory`、`footer_hardware_3`、`footer_legal_title` 等遗漏的 key

**批量修补脚本**：`scripts/patch-i18n-switcher.py`（可重复运行，幂等性）

---

**历史：2026-03-16 全量多语言注入：**

本次**全量多语言适配**（i18n_full_inject.py + i18n_inject_round2.py）在原有 285 键基础上大幅扩充，并完成两轮批量注入：

**翻译 key 总量**：从 285 键扩充至 **546 键**（新增 261 个 UI 翻译 key）

**HTML data-i18n 注入汇总**（两轮合计）：

| 目录 | 页面数 | 注入属性数 |
|------|-------|----------|
| home/ | 5 | ~120 |
| pdp/ | 4 | ~145 |
| catalog/ | 3 | ~87 |
| case-studies/ | 6 | ~54 |
| support/ | 4 | ~33 |
| landing/ | 3 | ~29 |
| quote/ | 3 | ~35 |
| thank-you/ | 2 | ~15 |
| **合计** | **30** | **~576** |

**语言文件同步**：全部 **25 种语言**的 `*-ui.json` 均已同步新 key（非 zh-CN/en 的 23 种语言新 key 暂用英文值占位，可后续通过 `npm run translate:products:incremental` 补全本地化翻译）。

**注入策略**：
- 基于 `en-ui.json` 的 value → key 反向映射，精确匹配 HTML 中的英文文本
- 仅处理 `<a>`、`<button>`、`<h1-h6>`、`<p>`、`<span>`、`<li>`、`<label>`、`<div>`、`<strong>`、`<em>`、`<small>` 等可见文本标签
- `<input>` / `<textarea>` 的 placeholder 属性使用 `data-i18n-placeholder` 注入
- 保护 `<script>` / `<style>` 块不被误修改
- 跳过少于 3 字符的字符串及纯数字/符号内容
- `emails/` 与 `linkedin/` 目录不参与（模板页面无需动态语言切换）

如需重新生成（例如 en-ui.json 新增键后），可重新运行注入脚本：
```bash
# 第一轮（基础 key 注入）
python3 /tmp/i18n_full_inject.py

# 第二轮（补充 key 注入）
python3 /tmp/i18n_inject_round2.py
```

### 语言初始化规则

```
1. 读取 localStorage.getItem('userLanguage')
2. 验证该代码在 lang-registry 的 languageNames 中存在
3. 有效 → 使用该语言
4. 无效或不存在 → 默认 zh-CN
```

---

## i18n 工具链命令

### 完整数据流

```
飞书多维表格（产品数据源）
  ↓ npm run sync:feishu
src/assets/product-data-table.js（产品原始数据）
  ↓ npm run i18n:extract
scripts/producti18n.json（提取出的 i18n 键）
  ↓ npm run product:sync:source
src/assets/lang/{lang}-product.json（中文源写入各语言文件）
  ↓ npm run translate:products:incremental
src/assets/lang/{lang}-product.json（Gemini API 翻译填充）
  ↓ npm run product:sync
src/assets/lang/{lang}-product.json（同步最终产品翻译）
  ↓ npm run product:collect
src/assets/lang/{lang}-product.json（按语言收集汇总）
  ↓ npm run split:lang
src/assets/lang/{lang}-ui.json + {lang}-product.json（拆分完成）
  ↓ webpack build
dist/assets/lang/（生产产物）
```

### 命令说明

```bash
# 从产品数据表提取 i18n 键（生成 producti18n.json）
npm run i18n:extract

# 合并所有语言翻译文件
npm run merge:i18n

# 按语言拆分为 {lang}-ui.json + {lang}-product.json
npm run split:lang

# 收集各语言产品翻译到统一文件
npm run product:collect

# 同步产品 i18n（仅同步中文源，不翻译）
npm run product:sync:source

# 同步产品 i18n（同步所有语言）
npm run product:sync

# 增量翻译（只翻译新增/变更的键）— 日常推荐
npm run translate:products:incremental

# 全量翻译（重新翻译全部键，耗时较长）
npm run translate:products
```

---

## 翻译引擎

### Gemini API 配置

翻译引擎封装在 `scripts/unified-translator.js`（60KB），通过 Gemini 3 API 实现批量多语言翻译。

配置方式（环境变量）：
```bash
GEMINI_API_KEY=your_api_key
GEMINI_BASE_URL=https://api.kuai.host   # 可选，默认官方地址
```

### 品牌词保护机制

`scripts/product-translation-handler.js` 在翻译前保护以下内容不被翻译：

- **品牌/产品型号**：ESL、ECOMAX、YuKoLi 等 20+ 品牌词
- **数字与单位**：`25kW`、`380V`、`50Hz`、`10-20kg/锅次` 等
- **Emoji**：直接保留原样
- **规格参数**：尺寸、重量等包含数字的字段

保护方式：翻译前用占位符替换敏感内容，翻译后还原。

### 增量翻译策略

`--incremental` 模式通过对比 `producti18n.json` 的哈希值，只翻译：
1. 新增的键
2. 中文原文发生变化的键

未变化的键直接复用现有翻译，大幅减少 API 调用量和翻译成本。

---

## 新增语言

### 完整步骤

**第一步：在 `src/lang-registry.js` 注册新语言**

```javascript
{
  code: 'vi',                    // 语言代码（BCP 47 格式）
  nativeName: 'Tiếng Việt',     // 前端弹窗显示的原生名称
  englishName: 'Vietnamese',     // Gemini 翻译 Prompt 中使用的英文名称
  hasTranslation: true,          // true = 参与翻译引擎
  uiGroup: 'asian',              // 'common' | 'european' | 'asian' | 'rtl'
  sortOrder: 21,                 // 弹窗排序（数字越小越靠前）
}
```

**第二步：创建空的 UI 翻译文件**

```bash
# 以 zh-CN-ui.json 为模板，创建新语言 UI 文件
cp src/assets/lang/zh-CN-ui.json src/assets/lang/vi-ui.json
# 将 vi-ui.json 中的中文值清空（保留键），或直接跑翻译
```

**第三步：同步并翻译产品数据**

```bash
npm run product:sync:source      # 同步中文源到新语言产品文件
npm run translate:products:incremental  # 调用 Gemini API 翻译
```

**第四步：验证**

```bash
npm run split:lang               # 拆分文件
npm start                        # 启动开发服务器，切换到新语言验证
```

---

## 常见问题排查

### 语言切换后页面文本没有变化

1. 打开浏览器控制台，检查是否有 `Failed to load translations` 报错
2. 确认 `dist/assets/lang/` 或 `src/assets/lang/` 下存在对应文件：
   ```bash
   ls src/assets/lang/ | grep "^en-"
   # 应输出：en-product.json  en-ui.json
   ```
3. 确认 `src/lang-registry.js` 中该语言的 `hasTranslation: true`

### 翻译文件存在但产品名称显示中文

该语言的 `-product.json` 为空 `{}`，属于预期 fallback 行为。  
执行以下命令生成翻译：
```bash
npm run translate:products:incremental
```

### 新增了 i18n 键但页面不显示

1. 确认 HTML 中使用了 `data-i18n="key_name"` 属性
2. 确认该键已添加到 `zh-CN-ui.json`（作为数据源）
3. 执行 `npm run merge:i18n && npm run split:lang` 同步到其他语言

### RTL 语言（阿拉伯语/希伯来语）布局问题

`ar` 和 `he` 属于 `rtl` 分组，语言切换时会自动为 `<html>` 设置 `dir="rtl"`，CSS 通过 `[dir="rtl"]` 选择器处理布局镜像。
